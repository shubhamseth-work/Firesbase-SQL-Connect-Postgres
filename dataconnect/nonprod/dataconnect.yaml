# ============================================
# FIREBASE DATA CONNECT - NONPROD
# ============================================
specVersion: "v1beta"

serviceId: "clsql-react-nonprod-dataconnect"

location: "us-central1"

schema:
  source: "../schema.gql"
  datasource:
    postgresql:
      database: "employee_db"
      cloudSql:
        instanceId: "clsql-react-nonprod-db"

connectorDirs:
  - "../connectors"

generate:
  javascriptSdk:
    outputDir: "../../src/generated/dataconnect"
    package:   "@clsql/dataconnect"
    packageJsonDir: "../../"